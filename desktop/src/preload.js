// Preload script. The renderer just loads the server-rendered web app over HTTP, so there's no
// privileged bridge to expose yet. Kept as a placeholder for future native integrations (#83) —
// e.g. exposing a minimal `window.scryme` API via contextBridge for file dialogs / deep links.
